{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ML Suite","text":"<p>You have a cool ML project idea. Your data might be 1D signals today, 2D images tomorrow, and 3D volumes after that. <code>ml_suite</code> gives you one set of building blocks that handles all three without rewriting your architecture code.</p> <p><code>ml_suite</code> provides model components only. It has no training loop, no logging, and no data loading. Users who want those should look at Lightning or a similar framework.</p>"},{"location":"#where-to-start","title":"Where to start","text":"<p>Pick the tab that matches your goal. Each one shows the recommended model and a minimal working example. The API links at the bottom of each tab have the full parameter reference.</p> I want to classify somethingI want a diffusion / flow modelI want a patch / ViT modelI want to build something custom <p>Use <code>ConvNet</code> when your input is a spatial grid like images, 1D signals, or 3D volumes. Use <code>make_point_cloud_classifier</code> when your input is an unordered set of points, since the transformer is permutation-invariant and order does not matter.</p> <p>Both return a logit vector of shape <code>(batch, num_classes)</code>.</p> <pre><code>from ml_suite.models.convolution import ConvNet\nfrom ml_suite.models.transformer.presets import make_point_cloud_classifier\n\n# 2-D image classifier (e.g. CIFAR-10)\nmodel = ConvNet(conv_dim=2, in_channels=3, stage_channels=[64, 128, 256],\n                blocks_per_stage=2, num_classes=10)\n\n# Permutation-invariant point-cloud classifier (e.g. ModelNet40)\nmodel = make_point_cloud_classifier(point_dim=3, num_classes=40,\n                                    embedding_dim=256, depth=6, num_heads=4)\n</code></pre> <p>See: Convolution \u00b7 Transformer presets</p> <p>These models learn to predict a denoising direction (DDPM) or a velocity field (flow matching) and require a time embedding at every forward pass.</p> <p>Use <code>ConditionedUNet</code> for spatial data with local structure like images or voxels. Use <code>make_conditioned_point_to_point_model</code> for point sets or sequences where you want a per-token output of the same shape as the input.</p> <pre><code>from ml_suite.models.unet import ConditionedUNet\nfrom ml_suite.models.transformer.presets import make_conditioned_point_to_point_model\n\n# Time-conditioned 2-D U-Net (DDPM / EDM style). Output shape matches input.\nmodel = ConditionedUNet(conv_dim=2, in_channels=3, out_channels=3,\n                        stage_channels=[64, 128, 256, 512],\n                        time_conditioning=True)\nout = model(x, time=t)  # x: (B, 3, H, W), t: (B,)\n\n# Flow-matching velocity field on point sets. Output shape matches input.\nmodel = make_conditioned_point_to_point_model(\n    point_dim=3, output_dim=3, embedding_dim=128,\n    depth=6, num_heads=4, time_conditioning=True)\nout = model(x, time=t)  # x: (B, N, 3), t: (B,)\n</code></pre> <p>See: U-Net \u00b7 Transformer presets</p> <p><code>PatchTransformerND</code> splits a spatial grid into non-overlapping patches, encodes them with a transformer, and can produce different output shapes depending on your task:</p> <ul> <li>Grid reconstruction (<code>make_patch_grid_model</code>): output has the same spatial shape as the input, useful for segmentation or denoising backbones.</li> <li>Classification (<code>make_patch_classifier</code>): a single pooled class vector, the standard ViT setup.</li> </ul> <p>Both work on 1-D, 2-D, and 3-D grids without changing the API.</p> <pre><code>from ml_suite.models.transformer.presets import make_patch_grid_model, make_patch_classifier\n\n# ViT-style image-to-image (e.g. segmentation backbone)\nmodel = make_patch_grid_model(input_dim=2, in_channels=3, out_channels=1,\n                              patch_size=16, embedding_dim=256,\n                              depth=12, num_heads=8)\n\n# ViT classifier (e.g. ImageNet)\nmodel = make_patch_classifier(input_dim=2, in_channels=3, num_classes=1000,\n                              patch_size=16, embedding_dim=768,\n                              depth=12, num_heads=12)\n</code></pre> <p>See: Transformer presets \u00b7 Transformer models</p> <p>The preset functions are thin wrappers around composable primitives. If you need a non-standard architecture with mixed modalities, custom conditioning, or unusual head shapes, you can wire the pieces together directly.</p> <p>The typical stack is tokenizer -&gt; transformer -&gt; head, with optional conditioning injected at the transformer level via FiLM or cross-attention.</p> <pre><code>from ml_suite.models.transformer import (\n    ContinuousInputTokenizer, TransformerStack,\n    PooledHead, RotaryEmbedding,\n)\nfrom ml_suite.models.linear import MLP\n\ntokenizer = ContinuousInputTokenizer(input_dim=64, embedding_dim=256)\nstack = TransformerStack(embedding_dim=256, depth=6, num_heads=8)\nhead = PooledHead(embedding_dim=256, output_dim=10)\n\ntokens = tokenizer(x)       # (B, N, 256)\nencoded = stack(tokens)     # (B, N, 256)\nout = head(encoded)         # (B, 10)\n</code></pre> <p>See: Transformer primitives \u00b7 Linear</p>"},{"location":"#module-map","title":"Module map","text":"Module What it provides Dimensionality Typical use <code>models.linear</code> Linear blocks, MLPs, FiLM / cross-attn conditioning any Conditioning blocks and MLP heads <code>models.convolution</code> Conv blocks, separable variants, multi-stage backbones 1D / 2D / 3D Classifiers on spatial data <code>models.unet</code> Encoder-decoder U-Nets with skip connections and rich conditioning 1D / 2D / 3D Generation and reconstruction <code>models.transformer</code> Token-centric transformers: classification, generation, patch grids any Sequences, point clouds, and patches <code>models.transformer.presets</code> One-call factory functions for common configurations any Quick model construction for common tasks <code>utils</code> Activation factory, sinusoidal / MLP time embeddings any Time embeddings for diffusion and flow models"},{"location":"#installation","title":"Installation","text":"uv (recommended)pip <pre><code>git clone https://github.com/DebajyotiS/ml_suite\ncd ml_suite\nuv sync --extra dev\n</code></pre> <pre><code>git clone https://github.com/DebajyotiS/ml_suite\ncd ml_suite\npip install -e \".[dev]\"\n</code></pre> <p>Requires Python &gt;= 3.13 and PyTorch &gt;= 2.12.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>By the end of this page you will be able to construct, run a forward pass through, and take a training step with any of the four model families in ml_suite. The examples assume basic familiarity with PyTorch tensors and <code>nn.Module</code>. You only need to read the sections that match your task.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"uv (recommended)pip <pre><code>git clone https://github.com/DebajyotiS/ml_suite\ncd ml_suite\nuv sync --extra dev\n</code></pre> <pre><code>git clone https://github.com/DebajyotiS/ml_suite\ncd ml_suite\npip install -e \".[dev]\"\n</code></pre> <p>Requirements: Python &gt;= 3.13, PyTorch &gt;= 2.12</p>"},{"location":"getting-started/#core-concepts","title":"Core concepts","text":"<p>ml_suite is organised into four model families. Every family is dimension-agnostic. You pick <code>1</code>, <code>2</code>, or <code>3</code> at construction time and the same class handles signals, images, or volumes.</p> Family Entry point Key parameter Linear / MLP <code>MLP</code>, <code>LinearBlock</code> <code>input_dim</code>, <code>output_dim</code> Convolution <code>ConvNet</code>, <code>ConditionedConvNet</code> <code>conv_dim</code> in U-Net <code>UNet</code>, <code>ConditionedUNet</code> <code>conv_dim</code> in Transformer <code>TokenToToken...</code>, <code>PatchTransformerND</code> <code>input_dim</code> in {1, 2, 3} (patch only) <p>Conditioning follows the same pattern everywhere: pass <code>time</code>, <code>class_labels</code>, <code>global_context</code>, or <code>cross_context</code> to the <code>forward</code> call of any <code>Conditioned*</code> model.</p>"},{"location":"getting-started/#example-image-classifier","title":"Example: image classifier","text":"<pre><code>import torch\nimport torch.nn.functional as F\nfrom ml_suite.models.convolution import ConvNet\n\nmodel = ConvNet(\n    conv_dim=2,\n    in_channels=3,\n    stage_channels=[64, 128, 256, 512],\n    blocks_per_stage=2,\n    num_classes=10,\n)\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-3)\n\nx = torch.randn(8, 3, 64, 64)\nlabels = torch.randint(0, 10, (8,))\nlogits = model(x)   # (8, 10)\n\nloss = F.cross_entropy(logits, labels)\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()\nprint(loss.item())  # should be around 2.3 for random data, just verifying the forward and backward pass run\n</code></pre>"},{"location":"getting-started/#example-diffusion-u-net","title":"Example: diffusion U-Net","text":"<pre><code>import torch\nimport torch.nn.functional as F\nfrom ml_suite.models.unet import ConditionedUNet\n\nmodel = ConditionedUNet(\n    conv_dim=2,\n    in_channels=3,\n    out_channels=3,\n    stage_channels=[64, 128, 256, 512],\n    time_conditioning=True,\n    attention_downsample_factors=[4, 8],\n)\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-4)\n\nx = torch.randn(4, 3, 64, 64)\nt = torch.rand(4)\ntarget = torch.randn(4, 3, 64, 64)\nout = model(x, time=t)   # (4, 3, 64, 64)\n\nloss = F.mse_loss(out, target)\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()\nprint(loss.item())  # should be around 2.0 for random data, just verifying the forward and backward pass run\n</code></pre>"},{"location":"getting-started/#example-point-cloud-flow-model","title":"Example: point-cloud flow model","text":"<pre><code>import torch\nimport torch.nn.functional as F\nfrom ml_suite.models.transformer.presets import make_conditioned_point_to_point_model\n\nmodel = make_conditioned_point_to_point_model(\n    point_dim=3,\n    output_dim=3,\n    embedding_dim=128,\n    depth=6,\n    num_heads=4,\n    time_conditioning=True,\n)\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-4)\n\nx = torch.randn(4, 1024, 3)   # (batch, points, xyz)\nt = torch.rand(4)\ntarget = torch.randn(4, 1024, 3)\nvelocity = model(x, time=t)   # (4, 1024, 3)\n\nloss = F.mse_loss(velocity, target)\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()\nprint(loss.item())  # should be around 2.0 for random data, just verifying the forward and backward pass run\n</code></pre>"},{"location":"getting-started/#example-vit-patch-model","title":"Example: ViT patch model","text":"<pre><code>import torch\nimport torch.nn.functional as F\nfrom ml_suite.models.transformer import PatchTransformerND\n\nmodel = PatchTransformerND(\n    input_dim=2,\n    in_channels=3,\n    out_channels=1,\n    patch_size=16,\n    embedding_dim=256,\n    depth=6,\n    num_heads=8,\n    output_mode=\"grid\",\n)\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-4)\n\nx = torch.randn(2, 3, 256, 256)\ntarget = torch.randn(2, 1, 256, 256)\nout = model(x)   # (2, 1, 256, 256)\n\nloss = F.mse_loss(out, target)\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()\nprint(loss.item())  # should be around 2.0 for random data, just verifying the forward and backward pass run\n</code></pre>"},{"location":"getting-started/#conditioning-cheat-sheet","title":"Conditioning cheat sheet","text":"<p>All <code>Conditioned*</code> models accept the same optional kwargs in <code>forward</code>:</p> Argument Shape When to use <code>time</code> <code>(batch,)</code> Diffusion / flow timestep <code>class_labels</code> <code>(batch,)</code> int Class-conditional generation <code>global_context</code> <code>(batch, context_dim)</code> Any global vector embedding <code>cross_context</code> <code>(batch, tokens, dim)</code> Text / cross-attention conditioning <code>cross_context_mask</code> <code>(batch, tokens)</code> bool Padding mask for <code>cross_context</code>"},{"location":"getting-started/#known-gotchas","title":"Known gotchas","text":"<ol> <li> <p>Group norm channel counts. Group norm requires channel counts divisible by <code>num_groups</code>. The default is <code>num_groups=32</code>. If your <code>stage_channels</code> values are not divisible by 32 you will get a wrong-shape error. Either set <code>num_groups=1</code> or pick channels that are multiples of 32 (64, 128, 256 work).</p> </li> <li> <p>Attention is off by default. <code>attention_downsample_factors</code> defaults to an empty tuple, which means no attention is added. This is correct for quick tests, but real diffusion models typically add attention at the bottleneck. Set it to something like <code>[4, 8]</code> to add attention at those downsample factors.</p> </li> <li> <p>Time is always required when enabled. For <code>ConditionedUNet</code> with <code>time_conditioning=True</code>, you must pass <code>time=t</code> at every forward call. Omitting it does not raise an error at construction time.</p> </li> <li> <p>Cross-attention context shape. <code>context_injection='cross_attn'</code> in <code>LinearBlock</code> expects a context tensor with a sequence dimension: shape <code>(batch, tokens, dim)</code>. All other injection modes expect <code>(batch, dim)</code>. Passing the wrong shape will silently broadcast incorrectly.</p> </li> </ol>"},{"location":"getting-started/#where-to-go-from-here","title":"Where to go from here","text":"<p>If you want to read the full parameter reference for a specific component, go to the API reference. If you want to wire primitives together without using a preset, start with the transformer primitives page. The preset factory functions page shows all one-liner constructors.</p>"},{"location":"recipes/","title":"Recipes","text":"<p>Short, working code patterns for common tasks. No parameter explanations here. If you need those, see the API reference.</p>"},{"location":"recipes/#training-a-flow-matching-model-on-images","title":"Training a flow-matching model on images","text":"<p>Flow matching trains a model to predict the velocity field that moves noise toward data along a straight path. The loss is the MSE between the predicted velocity and the target velocity at a random interpolation time.</p> <pre><code>import torch\nimport torch.nn.functional as F\nfrom ml_suite.models.unet import ConditionedUNet\n\nmodel = ConditionedUNet(\n    conv_dim=2,\n    in_channels=3,\n    out_channels=3,\n    stage_channels=[64, 128, 256, 512],\n    time_conditioning=True,\n    attention_downsample_factors=[4, 8],\n)\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-4)\n\nbatch_size = 4\nx = torch.randn(batch_size, 3, 64, 64)   # real data sample\nnoise = torch.randn_like(x)\nt = torch.rand(batch_size)               # random time in [0, 1]\nt_view = t.view(-1, 1, 1, 1)\n\nx_t = (1 - t_view) * noise + t_view * x  # linear interpolation\nvelocity_target = x - noise               # constant velocity field\n\nvelocity_pred = model(x_t, time=t)\nloss = F.mse_loss(velocity_pred, velocity_target)\n\noptimizer.zero_grad()\nloss.backward()\noptimizer.step()\nprint(loss.item())\n</code></pre>"},{"location":"recipes/#using-the-same-model-on-1d-and-2d-data","title":"Using the same model on 1D and 2D data","text":"<p>The only change between a 1D and 2D model is <code>conv_dim</code>. Everything else, including conditioning, training loop, and loss, stays the same.</p> <pre><code>import torch\nfrom ml_suite.models.unet import ConditionedUNet\n\n# 1D: audio or time-series denoiser\nmodel_1d = ConditionedUNet(\n    conv_dim=1,\n    in_channels=1,\n    out_channels=1,\n    stage_channels=[32, 64, 128],\n    time_conditioning=True,\n)\n\n# 2D: image denoiser. Only conv_dim changes.\nmodel_2d = ConditionedUNet(\n    conv_dim=2,\n    in_channels=1,\n    out_channels=1,\n    stage_channels=[32, 64, 128],\n    time_conditioning=True,\n)\n\nt = torch.rand(4)\n\nsignal = torch.randn(4, 1, 1024)\nout_1d = model_1d(signal, time=t)   # (4, 1, 1024)\n\nimage = torch.randn(4, 1, 64, 64)\nout_2d = model_2d(image, time=t)    # (4, 1, 64, 64)\n</code></pre>"},{"location":"recipes/#adding-multiple-conditioning-signals","title":"Adding multiple conditioning signals","text":"<p>Pass <code>time_conditioning=True</code> and <code>num_classes=N</code> at construction time, then pass both <code>time</code> and <code>class_labels</code> at every forward call.</p> <pre><code>import torch\nfrom ml_suite.models.unet import ConditionedUNet\n\nmodel = ConditionedUNet(\n    conv_dim=2,\n    in_channels=3,\n    out_channels=3,\n    stage_channels=[64, 128, 256, 512],\n    time_conditioning=True,\n    num_classes=10,\n    attention_downsample_factors=[4, 8],\n)\n\nx = torch.randn(4, 3, 64, 64)\nt = torch.rand(4)\ny = torch.randint(0, 10, (4,))\n\nout = model(x, time=t, class_labels=y)   # (4, 3, 64, 64)\n</code></pre>"},{"location":"recipes/#building-a-custom-architecture-from-primitives","title":"Building a custom architecture from primitives","text":"<p>The standard pattern is tokenizer \u2192 transformer stack \u2192 head. Use this when no preset fits your architecture.</p> <pre><code>import torch\nfrom ml_suite.models.transformer import (\n    ContinuousInputTokenizer,\n    TransformerStack,\n    PooledHead,\n)\n\n# Step 1: project raw features into the transformer's embedding space\ntokenizer = ContinuousInputTokenizer(input_dim=64, embedding_dim=256)\n\n# Step 2: apply the transformer layers\nstack = TransformerStack(embedding_dim=256, depth=6, num_heads=8)\n\n# Step 3: collapse the token sequence to a single vector for the task head\nhead = PooledHead(embedding_dim=256, output_dim=10)\n\nx = torch.randn(8, 32, 64)   # (batch, tokens, features)\ntokens = tokenizer(x)         # (8, 32, 256)\nencoded = stack(tokens)       # (8, 32, 256)\nout = head(encoded)           # (8, 10)\n</code></pre>"},{"location":"api/convolution/","title":"Convolution models","text":"<p><code>ml_suite.models.convolution</code> contains blocks and multi-stage backbones that work identically for 1D signals, 2D images, and 3D volumes. The <code>conv_dim</code> parameter is the only thing that changes between them.</p> <p>ConvNet vs UNet. For classification and feature extraction, use <code>ConvNet</code>. For generation and reconstruction tasks where output spatial size needs to match input size, use <code>ConditionedUNet</code> from <code>models.unet</code>. The U-Net has skip connections that preserve spatial detail through the decoder. The ConvNet is encoder-only.</p> <p>ConvNet vs ConditionedConvNet. Use <code>ConvNet</code> for pure discriminative tasks like classification. Use <code>ConditionedConvNet</code> when you need to inject a global signal, such as a class embedding or a diffusion timestep, into every convolutional layer via FiLM.</p> <p>Receptive field diagnostic. <code>ConvNet</code> exposes a <code>print_receptive_field</code> method. When you are tuning <code>stage_channels</code> and <code>blocks_per_stage</code> and want to know how large a spatial region each output feature represents, call this method. It prints the cumulative receptive field after each layer so you can verify that the field covers your target structure size.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks","title":"blocks","text":"<p>Dimension-agnostic convolutional primitives and ConvNet backbones.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConvBlock","title":"ConvBlock","text":"<pre><code>ConvBlock(input_channels: int, output_channels: int, conv_dim: int = 2, kernel_size: int = 3, stride: int = 1, padding: int = 1, activation: str = 'silu', norm_type: NormType = 'batch', num_groups: int = 32, do_residual: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>A dimension-agnostic convolutional block.</p> Operation order <p>Conv -&gt; Norm -&gt; Residual Add -&gt; Activation</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConvBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Apply conv, norm, optional residual, and activation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, input_channels, *spatial).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, output_channels, *spatial).</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConditionedConvBlock","title":"ConditionedConvBlock","text":"<pre><code>ConditionedConvBlock(input_channels: int, output_channels: int, context_dim: int, **kwargs)\n</code></pre> <p>               Bases: <code>ConvBlock</code></p> <p>A ConvBlock with FiLM conditioning.</p> <p>The context vector has shape <code>(batch, context_dim)</code> and is projected into per-channel scale and shift parameters. The FiLM projection is zero-initialized, so the block starts as the matching unconditioned ConvBlock.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConditionedConvBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor) -&gt; Tensor\n</code></pre> <p>Apply conv, norm, FiLM conditioning, optional residual, and activation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, input_channels, *spatial).</p> required <code>context</code> <code>Tensor</code> <p>Conditioning vector of shape (batch, context_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, output_channels, *spatial).</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.SeparableConvBlock","title":"SeparableConvBlock","text":"<pre><code>SeparableConvBlock(input_channels: int, output_channels: int, conv_dim: int = 2, kernel_size: int = 3, stride: int = 1, padding: int = 1, activation: str = 'silu', norm_type: NormType = 'batch', num_groups: int = 32, do_residual: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Depthwise separable convolution block (depthwise conv + pointwise conv).</p> <p>Reduces FLOPs by roughly k\u00b2 relative to a full ConvBlock of the same shape. The depthwise conv handles spatial mixing; the pointwise 1x1 handles channel mixing. Norm and activation are applied after the pointwise step, matching the ConvBlock operation order.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.SeparableConvBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Apply depthwise conv, pointwise conv, norm, optional residual, and activation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, input_channels, *spatial).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, output_channels, *spatial).</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.SeparableConditionedConvBlock","title":"SeparableConditionedConvBlock","text":"<pre><code>SeparableConditionedConvBlock(input_channels: int, output_channels: int, context_dim: int, **kwargs)\n</code></pre> <p>               Bases: <code>SeparableConvBlock</code></p> <p>SeparableConvBlock with FiLM conditioning.</p> <p>The context vector <code>(batch, context_dim)</code> is projected into per-channel scale and shift applied after the pointwise conv and norm. Zero-initialised projection ensures the block starts as a plain SeparableConvBlock.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.SeparableConditionedConvBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor) -&gt; Tensor\n</code></pre> <p>Apply separable conv, norm, FiLM conditioning, optional residual, and activation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, input_channels, *spatial).</p> required <code>context</code> <code>Tensor</code> <p>Conditioning vector of shape (batch, context_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, output_channels, *spatial).</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConvNet","title":"ConvNet","text":"<pre><code>ConvNet(conv_dim: int, in_channels: int, stage_channels: Sequence[int], blocks_per_stage: int | Sequence[int], downsample_mode: DownsampleMode = 'stride', norm_type: NormType = 'batch', global_pool_mode: GlobalPoolMode = 'avg', num_classes: int | None = None, activation: str = 'silu', num_groups: int = 32, conv_type: ConvType = 'standard')\n</code></pre> <p>               Bases: <code>Module</code></p> <p>A flexible 1D, 2D, or 3D convolutional backbone or classifier.</p> <p>Examples:</p> <p>Feature extractor (no <code>num_classes</code>):</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.convolution.blocks import ConvNet\n&gt;&gt;&gt; model = ConvNet(\n...     conv_dim=2, in_channels=3, stage_channels=[64, 128, 256],\n...     blocks_per_stage=2\n... )\n&gt;&gt;&gt; x = torch.randn(4, 3, 64, 64)\n&gt;&gt;&gt; features = model(x)  # (4, 256) \u2014 globally pooled feature vector\n</code></pre> <p>Classifier (with <code>num_classes</code>):</p> <pre><code>&gt;&gt;&gt; model = ConvNet(\n...     conv_dim=2, in_channels=3, stage_channels=[64, 128, 256],\n...     blocks_per_stage=2, num_classes=10\n... )\n&gt;&gt;&gt; logits = model(x)\n&gt;&gt;&gt; logits.shape\ntorch.Size([4, 10])\n</code></pre>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConvNet.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Run the stem, all stages, and the optional classification head.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, in_channels, *spatial).</p> required <p>Returns:</p> Name Type Description <code>Tensor</code> <p>If num_classes is set: logits of shape (batch, num_classes).</p> <code>Otherwise</code> <code>Tensor</code> <p>feature map of shape (batch, stage_channels[-1], *spatial).</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConvNet.print_receptive_field","title":"print_receptive_field","text":"<pre><code>print_receptive_field() -&gt; None\n</code></pre> <p>Print the analytical receptive field after each convolution or pool.</p> <p>Tracks cumulative receptive field and cumulative stride across the convolutional backbone. This is a diagnostic utility only.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConditionedConvNet","title":"ConditionedConvNet","text":"<pre><code>ConditionedConvNet(conv_dim: int, in_channels: int, stage_channels: Sequence[int], blocks_per_stage: int | Sequence[int], context_dim: int, downsample_mode: DownsampleMode = 'stride', norm_type: NormType = 'batch', global_pool_mode: GlobalPoolMode = 'avg', num_classes: int | None = None, activation: str = 'silu', num_groups: int = 32, conv_type: ConvType = 'standard')\n</code></pre> <p>               Bases: <code>ConvNet</code></p> <p>FiLM-conditioned ConvNet using ConditionedConvBlock in all stages.</p>"},{"location":"api/convolution/#ml_suite.models.convolution.blocks.ConditionedConvNet.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor) -&gt; Tensor\n</code></pre> <p>Run the FiLM-conditioned stem, all stages, and the optional classification head.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, in_channels, *spatial).</p> required <code>context</code> <code>Tensor</code> <p>Global conditioning vector of shape (batch, context_dim).</p> required <p>Returns:</p> Name Type Description <code>Tensor</code> <p>If num_classes is set: logits of shape (batch, num_classes).</p> <code>Otherwise</code> <code>Tensor</code> <p>feature map of shape (batch, stage_channels[-1], *spatial).</p>"},{"location":"api/linear/","title":"Linear models","text":"<p><code>ml_suite.models.linear</code> provides linear projection blocks and multi-layer perceptrons with optional context conditioning via concatenation, addition, multiplication, FiLM, or cross-attention.</p>"},{"location":"api/linear/#linear-blocks","title":"Linear blocks","text":"<p><code>LinearBlock</code> is the basic unit for MLP-style networks in this library. It wraps a single linear projection and optionally accepts a secondary conditioning tensor. The <code>context_injection</code> parameter controls how that second signal is mixed in. For most conditioning tasks, <code>film</code> is the right choice because it applies a learned scale and shift per feature, which is what diffusion models use to inject time information. Use <code>concat</code> when you want the simplest possible thing and are happy for the network to learn the mixing from scratch.</p> <p>Choosing <code>context_injection</code>: <code>concat</code> appends context to the input before projection, giving the network full control over the mixing. <code>add</code> and <code>multiply</code> are element-wise and useful when context and input are already in the same space. <code>film</code> applies learned scale and shift and is the standard choice for diffusion timestep conditioning. <code>cross_attn</code> applies multi-head attention and requires context to have shape <code>(batch, tokens, dim)</code> rather than <code>(batch, dim)</code>.</p>"},{"location":"api/linear/#ml_suite.models.linear.blocks","title":"blocks","text":"<p>Linear transformation primitives and MLP stacks.</p>"},{"location":"api/linear/#ml_suite.models.linear.blocks.LinearBlock","title":"LinearBlock","text":"<pre><code>LinearBlock(input_dim: int, output_dim: int, context_dim: int = 0, context_injection: Literal['concat', 'add', 'multiply', 'film', 'cross_attn'] = 'concat', activation: str = 'silu', do_residual: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>A linear transformation primitive supporting context conditioning and residual routing.</p> <p>This block wraps a standard linear mapping while offering multiple ways to inject secondary context tensors (e.g., conditioning tokens, embeddings, or timesteps). The layer ordering executes as follows: Context Conditioning -&gt; Linear Projection -&gt; Residual Addition -&gt; Activation.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Number of features in the input tensor.</p> required <code>output_dim</code> <code>int</code> <p>Number of features produced by the main linear projection.</p> required <code>context_dim</code> <code>int</code> <p>Number of features in the conditioning context tensor. Set to 0 to disable.</p> <code>0</code> <code>context_injection</code> <code>Literal['concat', 'add', 'multiply', 'film', 'cross_attn']</code> <p>The mechanism used to merge the context tensor into the main stream: - 'concat': Concatenates input and context along the feature dimension before   projection. - 'add': Projects context to match input features and sums them. - 'multiply': Projects context to match input features and multiplies them. - 'film': Computes feature-wise affine scaling and shifting parameters from the context. - 'cross_attn': Applies a multi-head cross-attention mechanism over the context.</p> <code>'concat'</code> <code>activation</code> <code>str</code> <p>Name of the activation function to apply.</p> <code>'silu'</code> <code>do_residual</code> <code>bool</code> <p>If True, adds the input tensor to the linear projection output.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>do_residual</code> is True but <code>input_dim</code> does not equal <code>output_dim</code>.</p> <code>ValueError</code> <p>If an unsupported <code>context_injection</code> strategy is provided.</p> <p>Examples:</p> <p>Basic projection with no conditioning:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.linear.blocks import LinearBlock\n&gt;&gt;&gt; block = LinearBlock(input_dim=64, output_dim=128)\n&gt;&gt;&gt; x = torch.randn(4, 64)\n&gt;&gt;&gt; out = block(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 128])\n</code></pre> <p>FiLM conditioning (e.g. diffusion timestep injection):</p> <pre><code>&gt;&gt;&gt; block = LinearBlock(\n...     input_dim=64, output_dim=64, context_dim=32, context_injection='film'\n... )\n&gt;&gt;&gt; ctx = torch.randn(4, 32)\n&gt;&gt;&gt; out = block(x, ctx)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 64])\n</code></pre>"},{"location":"api/linear/#ml_suite.models.linear.blocks.LinearBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply context conditioning, linear projection, optional residual, and activation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (..., input_dim).</p> required <code>context</code> <code>Tensor | None</code> <p>Conditioning tensor. Required when context_dim &gt; 0. For 'concat': shape (..., context_dim). For 'add'/'multiply': shape (..., context_dim). For 'film': shape (..., context_dim). For 'cross_attn': shape (..., context_dim).</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (..., output_dim).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If context_dim &gt; 0 and context is None.</p>"},{"location":"api/linear/#ml_suite.models.linear.blocks.MLP","title":"MLP","text":"<pre><code>MLP(input_dim: int, hidden_dim: int, num_layers: int, context_dim: int = 0, context_injection: Literal['concat', 'add', 'multiply', 'film', 'cross_attn'] = 'concat', hidden_layers: list[int] | None = None, activation: str = 'silu', activation_list: list[str] | None = None, do_residual: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>A Multi-Layer Perceptron network composed of sequential LinearBlock primitives.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Feature dimension entering the first network layer.</p> required <code>hidden_dim</code> <code>int</code> <p>Default feature dimension produced by the final output layer block.</p> required <code>num_layers</code> <code>int</code> <p>Total number of LinearBlock layers to create. Must be at least 1.</p> required <code>context_dim</code> <code>int</code> <p>Feature dimension of the conditioning tensor. Set to 0 to disable.</p> <code>0</code> <code>context_injection</code> <code>Literal['concat', 'add', 'multiply', 'film', 'cross_attn']</code> <p>The mechanism used to merge context vectors inside each block.</p> <code>'concat'</code> <code>hidden_layers</code> <code>list[int] | None</code> <p>Sequence of custom feature hidden dimensions between layers. Must contain exactly <code>num_layers - 1</code> elements. If None, defaults to <code>hidden_dim</code>.</p> <code>None</code> <code>activation</code> <code>str</code> <p>Default activation name applied across all layer blocks.</p> <code>'silu'</code> <code>activation_list</code> <code>list[str] | None</code> <p>Explicit list of activation names for each layer. Must contain exactly <code>num_layers</code> elements.</p> <code>None</code> <code>do_residual</code> <code>bool</code> <p>If True, blocks with matching input and output sizes use residual connections.</p> <code>False</code> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.linear.blocks import MLP\n&gt;&gt;&gt; mlp = MLP(input_dim=32, hidden_dim=64, num_layers=3)\n&gt;&gt;&gt; x = torch.randn(8, 32)\n&gt;&gt;&gt; out = mlp(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([8, 64])\n</code></pre>"},{"location":"api/linear/#ml_suite.models.linear.blocks.MLP.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Pass x through all LinearBlock layers in sequence.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (..., input_dim).</p> required <code>context</code> <code>Tensor | None</code> <p>Optional conditioning tensor passed to every block unchanged.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (..., hidden_dim).</p>"},{"location":"api/linear/#vad-inference","title":"VAD inference","text":"<p>Not yet implemented</p> <p>The classes in this section are experimental placeholders. <code>VADLinearBlock.forward</code> raises <code>NotImplementedError</code>. The API may change before implementation is complete.</p>"},{"location":"api/linear/#ml_suite.models.linear.vad","title":"vad","text":"<p>Variational Adaptive Dropout (VAD) modules \u2014 experimental, not yet implemented.</p>"},{"location":"api/linear/#ml_suite.models.linear.vad.VADState","title":"VADState  <code>dataclass</code>","text":"<pre><code>VADState(mu: Tensor, var: Tensor)\n</code></pre>"},{"location":"api/linear/#ml_suite.models.linear.vad.VADState.from_tensor","title":"from_tensor  <code>staticmethod</code>","text":"<pre><code>from_tensor(mean: Tensor) -&gt; VADState\n</code></pre> <p>Construct a VADState from a mean tensor with variance initialised to zero.</p>"},{"location":"api/linear/#ml_suite.models.linear.vad.LinearVADInference","title":"LinearVADInference","text":"<pre><code>LinearVADInference(input_dim: int, output_dim: int)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Small MLP for inferring per-layer adaptive (input-dependent) dropout scale.</p>"},{"location":"api/linear/#ml_suite.models.linear.vad.LinearVADInference.forward","title":"forward","text":"<pre><code>forward(data: Tensor) -&gt; Tensor\n</code></pre> <p>Infer per-element adaptive dropout scale.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Tensor</code> <p>Input tensor of shape (batch, input_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Scale tensor of shape (batch, output_dim).</p>"},{"location":"api/linear/#ml_suite.models.linear.vad.VADLinearBlock","title":"VADLinearBlock","text":"<pre><code>VADLinearBlock(input_dim: int, output_dim: int, context_dim: int = 0, context_injection: Literal['concat', 'add', 'multiply', 'film', 'cross_attn'] = 'concat', activation: str = 'silu', do_residual: bool = False, p: float = 0.5)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Placeholder for a VAD-equipped linear block. Not yet implemented.</p>"},{"location":"api/unet/","title":"U-Net models","text":"<p><code>ml_suite.models.unet</code> provides encoder-decoder U-Nets for 1D, 2D, and 3D data with skip connections, optional self- and cross-attention, and a rich conditioning interface (time, class, global context).</p> <p>Most users will only need <code>UNet</code> or <code>ConditionedUNet</code>. Use <code>UNet</code> for tasks where the model takes only the data tensor as input, like segmentation. Use <code>ConditionedUNet</code> when the model needs additional signals at every layer, such as a diffusion timestep, a class label, or a global context vector. The internal stage, attention, and conditioning classes documented below are building blocks for custom architectures.</p>"},{"location":"api/unet/#models","title":"Models","text":""},{"location":"api/unet/#unet","title":"UNet","text":""},{"location":"api/unet/#ml_suite.models.unet.models.UNet","title":"UNet","text":"<pre><code>UNet(conv_dim: int, in_channels: int, out_channels: int, stage_channels: Sequence[int], blocks_per_stage: int | Sequence[int] = 2, downsample_mode: DownsampleMode = 'stride', upsample_mode: UpsampleMode = 'interpolate', skip_mode: SkipMode = 'concat', shape_policy: ShapePolicy = 'resize', norm_type: NormType = 'batch', activation: str = 'silu', num_groups: int = 32, output_activation: str | None = None, attention_downsample_factors: Sequence[int] = (), attention_locations: AttentionLocation = 'both', num_attention_heads: int = 4, attention_head_dim: int | None = None, attention_dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>BaseUNet</code></p> <p>A plain dimension-agnostic U-Net for 1D, 2D, or 3D data.</p> <p>Supports optional spatial self-attention. Time, class, global context, and cross-attention conditioning are handled by <code>ConditionedUNet</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.unet.models import UNet\n&gt;&gt;&gt; model = UNet(\n...     conv_dim=2, in_channels=3, out_channels=1,\n...     stage_channels=[64, 128, 256]\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 1, 64, 64])\n</code></pre>"},{"location":"api/unet/#ml_suite.models.unet.models.UNet.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Run the plain U-Net encoder-decoder forward pass.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, in_channels, *spatial).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, out_channels, *spatial).</p>"},{"location":"api/unet/#conditionedunet","title":"ConditionedUNet","text":"<p>All global conditioning sources, meaning <code>time</code>, <code>class_labels</code>, and <code>global_context</code>, are each projected to <code>condition_dim</code> and then summed into a single vector. That vector is passed to every convolutional block as a FiLM signal. The FiLM projection is zero-initialized, so at the start of training the model behaves identically to an unconditioned UNet and the conditioning signal is learned gradually. Token-level conditioning via <code>cross_context</code> goes only to the spatial attention blocks.</p> <p>Multi-signal forward examples:</p> <pre><code># Time-conditioned with class labels\nout = model(x, time=t, class_labels=labels)\n\n# With cross-attention context (e.g. text conditioning)\nout = model(x, time=t, cross_context=text_embeddings, cross_context_mask=mask)\n\n# All signals at once\nout = model(x, time=t, class_labels=labels,\n            global_context=ctx, cross_context=text_embeddings)\n</code></pre>"},{"location":"api/unet/#ml_suite.models.unet.models.ConditionedUNet","title":"ConditionedUNet","text":"<pre><code>ConditionedUNet(conv_dim: int, in_channels: int, out_channels: int, stage_channels: Sequence[int], blocks_per_stage: int | Sequence[int] = 2, downsample_mode: DownsampleMode = 'stride', upsample_mode: UpsampleMode = 'interpolate', skip_mode: SkipMode = 'concat', shape_policy: ShapePolicy = 'resize', norm_type: NormType = 'batch', activation: str = 'silu', num_groups: int = 32, output_activation: str | None = None, condition_dim: int | None = None, time_conditioning: bool = False, time_embedding_type: TimeEmbeddingType = 'sinusoidal', num_classes: int | None = None, class_dropout_prob: float = 0.0, global_context_dim: int | None = None, attention_downsample_factors: Sequence[int] = (), attention_locations: AttentionLocation = 'both', attention_type: AttentionType = 'self', num_attention_heads: int = 4, attention_head_dim: int | None = None, cross_attention_dim: int | None = None, attention_dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>BaseUNet</code></p> <p>A FiLM-conditioned dimension-agnostic U-Net with optional cross-attention.</p> <p>Global conditioning sources <code>time</code>, <code>class_labels</code>, and <code>global_context</code> are combined into one vector and passed to every conditioned convolutional block. Token conditioning <code>cross_context</code> is used only by attention blocks.</p> <p>Examples:</p> <p>Time-conditioned flow model:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.unet.models import ConditionedUNet\n&gt;&gt;&gt; model = ConditionedUNet(\n...     conv_dim=2, in_channels=3, out_channels=3,\n...     stage_channels=[64, 128, 256], time_conditioning=True\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)\n&gt;&gt;&gt; t = torch.rand(2)\n&gt;&gt;&gt; out = model(x, time=t)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 3, 64, 64])\n</code></pre> <p>Class-conditional generation:</p> <pre><code>&gt;&gt;&gt; model = ConditionedUNet(\n...     conv_dim=2, in_channels=3, out_channels=3,\n...     stage_channels=[64, 128, 256], time_conditioning=True, num_classes=10\n... )\n&gt;&gt;&gt; y = torch.randint(0, 10, (2,))\n&gt;&gt;&gt; out = model(x, time=t, class_labels=y)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 3, 64, 64])\n</code></pre>"},{"location":"api/unet/#ml_suite.models.unet.models.ConditionedUNet.forward","title":"forward","text":"<pre><code>forward(x: Tensor, time: Tensor | None = None, class_labels: Tensor | None = None, global_context: Tensor | None = None, cross_context: Tensor | None = None, cross_context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Run the conditioned U-Net forward pass.</p> <p>All enabled global conditioning sources are projected to condition_dim and summed into a single FiLM vector passed to every conditioned convolutional block. Token cross-context is used only by spatial attention blocks.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input tensor of shape (batch, in_channels, *spatial).</p> required <code>time</code> <code>Tensor | None</code> <p>Diffusion timestep of shape (batch,). Required when time_conditioning=True.</p> <code>None</code> <code>class_labels</code> <code>Tensor | None</code> <p>Integer class indices of shape (batch,). Required when num_classes is set.</p> <code>None</code> <code>global_context</code> <code>Tensor | None</code> <p>Global context vector of shape (batch, global_context_dim). Required when global_context_dim is set.</p> <code>None</code> <code>cross_context</code> <code>Tensor | None</code> <p>Token sequence of shape (batch, tokens, cross_attention_dim). Required when attention_type is 'cross' or 'self_cross'.</p> <code>None</code> <code>cross_context_mask</code> <code>Tensor | None</code> <p>Boolean valid-token mask of shape (batch, tokens). True indicates a valid (non-padded) token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, out_channels, *spatial).</p>"},{"location":"api/unet/#internal-components","title":"Internal components","text":"Stages Attention Conditioning"},{"location":"api/unet/#ml_suite.models.unet.stages","title":"stages","text":"<p>Reusable encoder and decoder stages for plain and conditioned U-Nets.</p>"},{"location":"api/unet/#ml_suite.models.unet.stages.UNetStage","title":"UNetStage","text":"<pre><code>UNetStage(input_channels: int, output_channels: int, conv_dim: int, num_blocks: int, block_cls: type[Module] = ConvBlock, condition_dim: int | None = None, first_stride: int = 1, norm_type: NormType = 'batch', activation: str = 'silu', num_groups: int = 32, attention: SpatialAttentionBlock | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>A stack of ConvBlock or ConditionedConvBlock modules plus optional attention.</p>"},{"location":"api/unet/#ml_suite.models.unet.stages.UNetStage.forward","title":"forward","text":"<pre><code>forward(x: Tensor, condition: Tensor | None = None, cross_context: Tensor | None = None, cross_context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply all conv blocks then the optional spatial attention block.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Feature map of shape (batch, input_channels, *spatial).</p> required <code>condition</code> <code>Tensor | None</code> <p>FiLM vector of shape (batch, condition_dim). Required for conditioned stages.</p> <code>None</code> <code>cross_context</code> <code>Tensor | None</code> <p>Cross-attention token sequence of shape (batch, tokens, cross_attention_dim).</p> <code>None</code> <code>cross_context_mask</code> <code>Tensor | None</code> <p>Boolean mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Feature map of shape (batch, output_channels, *spatial).</p>"},{"location":"api/unet/#ml_suite.models.unet.stages.UNetUpStage","title":"UNetUpStage","text":"<pre><code>UNetUpStage(input_channels: int, skip_channels: int, output_channels: int, conv_dim: int, num_blocks: int, block_cls: type[Module] = ConvBlock, condition_dim: int | None = None, upsample_mode: UpsampleMode = 'interpolate', skip_mode: SkipMode = 'concat', shape_policy: ShapePolicy = 'resize', norm_type: NormType = 'batch', activation: str = 'silu', num_groups: int = 32, attention: SpatialAttentionBlock | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>One decoder stage shared by plain and conditioned U-Nets.</p>"},{"location":"api/unet/#ml_suite.models.unet.stages.UNetUpStage.forward","title":"forward","text":"<pre><code>forward(x: Tensor, skip: Tensor, condition: Tensor | None = None, cross_context: Tensor | None = None, cross_context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Upsample x, merge with the skip connection, and apply the decoder stage.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Lower-resolution feature map of shape (batch, input_channels, *spatial).</p> required <code>skip</code> <code>Tensor</code> <p>Matching encoder skip tensor of shape (batch, skip_channels, *spatial_skip).</p> required <code>condition</code> <code>Tensor | None</code> <p>FiLM vector of shape (batch, condition_dim). Required for conditioned stages.</p> <code>None</code> <code>cross_context</code> <code>Tensor | None</code> <p>Cross-attention token sequence of shape (batch, tokens, cross_attention_dim).</p> <code>None</code> <code>cross_context_mask</code> <code>Tensor | None</code> <p>Boolean mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Feature map of shape (batch, output_channels, *spatial_skip).</p>"},{"location":"api/unet/#ml_suite.models.unet.attention","title":"attention","text":"<p>Spatial attention blocks for dimension-agnostic U-Nets.</p>"},{"location":"api/unet/#ml_suite.models.unet.attention.SpatialAttentionBlock","title":"SpatialAttentionBlock","text":"<pre><code>SpatialAttentionBlock(channels: int, attention_type: AttentionType = 'self', num_heads: int = 4, head_dim: int | None = None, cross_attention_dim: int | None = None, dropout: float = 0.0, ff_mult: int = 4)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Spatial attention supporting self-attention and cross-attention.</p> <p>A convolutional feature map <code>(B, C, *spatial)</code> is flattened to spatial tokens <code>(B, N, C)</code>, processed with attention, then reshaped back.</p>"},{"location":"api/unet/#ml_suite.models.unet.attention.SpatialAttentionBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor, cross_context: Tensor | None = None, cross_context_mask: Tensor | None = None, spatial_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p><code>(B, C, *spatial_dims)</code></p> required <code>cross_context</code> <code>Tensor | None</code> <p><code>(B, context_tokens, cross_attention_dim)</code></p> <code>None</code> <code>cross_context_mask</code> <code>Tensor | None</code> <p><code>(B, context_tokens)</code> bool, True = valid token</p> <code>None</code> <code>spatial_mask</code> <code>Tensor | None</code> <p><code>(B, *spatial_dims)</code> bool, True = visible patch. Passed as key_padding_mask to self-attention, e.g. for masked patch prediction.</p> <code>None</code>"},{"location":"api/unet/#ml_suite.models.unet.conditioning","title":"conditioning","text":"<p>Global conditioning modules for U-Nets.</p>"},{"location":"api/unet/#ml_suite.models.unet.conditioning.ConditioningBuilder","title":"ConditioningBuilder","text":"<pre><code>ConditioningBuilder(condition_dim: int, time_conditioning: bool = False, time_embedding_type: TimeEmbeddingType = 'sinusoidal', num_classes: int | None = None, class_dropout_prob: float = 0.0, global_context_dim: int | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Builds a single FiLM conditioning vector from time, class, and context.</p> <p>The returned tensor always has shape <code>(B, condition_dim)</code>.</p>"},{"location":"api/unet/#ml_suite.models.unet.conditioning.ConditioningBuilder.forward","title":"forward","text":"<pre><code>forward(batch_size: int, device: device, dtype: dtype, time: Tensor | None = None, class_labels: Tensor | None = None, global_context: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Build the combined FiLM conditioning vector.</p> <p>All enabled conditioning sources are projected to condition_dim and summed.</p> <p>Parameters:</p> Name Type Description Default <code>batch_size</code> <code>int</code> <p>Number of samples in the batch.</p> required <code>device</code> <code>device</code> <p>Target device for the output tensor.</p> required <code>dtype</code> <code>dtype</code> <p>Target dtype for the output tensor.</p> required <code>time</code> <code>Tensor | None</code> <p>Timestep tensor of shape (batch,). Required when time_conditioning=True.</p> <code>None</code> <code>class_labels</code> <code>Tensor | None</code> <p>Integer class indices of shape (batch,). Required when num_classes is set.</p> <code>None</code> <code>global_context</code> <code>Tensor | None</code> <p>Global context of shape (batch, global_context_dim). Required when global_context_dim is set.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Conditioning vector of shape (batch, condition_dim).</p>"},{"location":"api/utils/","title":"Utilities","text":"<p><code>ml_suite.utils</code> provides shared utilities used across model families. You do not need to use these directly in most cases because the <code>Conditioned*</code> models wire them up internally. They are exposed here for custom architectures that need to build conditioning pipelines by hand.</p>"},{"location":"api/utils/#activations","title":"Activations","text":"<p><code>get_activation</code> is a factory that returns an <code>nn.Module</code> for a named activation function. Pass the name as a string anywhere in the library that accepts an <code>activation</code> argument. The factory exists so you can configure activation choice via a config file or command-line flag without importing the activation class directly.</p>"},{"location":"api/utils/#ml_suite.utils.activations","title":"activations","text":"<p>Activation function factory.</p>"},{"location":"api/utils/#ml_suite.utils.activations.get_activation","title":"get_activation","text":"<pre><code>get_activation(activation: str) -&gt; Module\n</code></pre> <p>Return an activation function module by name.</p> <p>Parameters:</p> Name Type Description Default <code>activation</code> <code>str</code> <p>One of 'relu', 'leakyrelu', 'gelu', 'sigmoid', 'tanh', 'softmax', 'silu'/'swish', 'mish', 'identity'/'none'.</p> required <p>Returns:</p> Type Description <code>Module</code> <p>An nn.Module instance for the requested activation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the activation name is not recognised.</p>"},{"location":"api/utils/#time-embeddings","title":"Time embeddings","text":"<p>These classes convert a scalar timestep <code>t</code> (shape <code>(batch,)</code>) into a vector that can be used as a conditioning signal. <code>SinusoidalTimeEmbedding</code> produces a fixed sinusoidal encoding and is a good default. <code>TimeEmbeddingMLP</code> follows it with a learned projection, which gives the network more capacity to learn how to use the time signal. In practice, <code>TimeEmbeddingMLP</code> is what most diffusion and flow-matching models use.</p>"},{"location":"api/utils/#ml_suite.utils.conditioning","title":"conditioning","text":"<p>Shared time embedding primitives used across UNet and Transformer conditioning.</p>"},{"location":"api/utils/#ml_suite.utils.conditioning.SinusoidalTimeEmbedding","title":"SinusoidalTimeEmbedding","text":"<pre><code>SinusoidalTimeEmbedding(embedding_dim: int)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Map scalar time values of shape (B,) or (B, 1) to sinusoidal embeddings.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.utils.conditioning import SinusoidalTimeEmbedding\n&gt;&gt;&gt; emb = SinusoidalTimeEmbedding(embedding_dim=128)\n&gt;&gt;&gt; t = torch.rand(4)\n&gt;&gt;&gt; out = emb(t)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 128])\n</code></pre>"},{"location":"api/utils/#ml_suite.utils.conditioning.SinusoidalTimeEmbedding.forward","title":"forward","text":"<pre><code>forward(time: Tensor) -&gt; Tensor\n</code></pre> <p>Embed scalar time values into a sinusoidal representation.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>Tensor</code> <p>Timestep tensor of shape (batch,) or (batch, 1).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Sinusoidal embeddings of shape (batch, embedding_dim).</p>"},{"location":"api/utils/#ml_suite.utils.conditioning.TimeEmbeddingMLP","title":"TimeEmbeddingMLP","text":"<pre><code>TimeEmbeddingMLP(embedding_dim: int, embedding_type: TimeEmbeddingType = 'sinusoidal', hidden_mult: int = 4)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Project scalar time values to a target embedding dimension.</p> <p>Applies a sinusoidal embedding followed by a two-layer MLP. This is the standard time conditioning block used in most diffusion and flow-matching models.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.utils.conditioning import TimeEmbeddingMLP\n&gt;&gt;&gt; emb = TimeEmbeddingMLP(embedding_dim=256)\n&gt;&gt;&gt; t = torch.rand(4)\n&gt;&gt;&gt; out = emb(t)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 256])\n</code></pre>"},{"location":"api/utils/#ml_suite.utils.conditioning.TimeEmbeddingMLP.forward","title":"forward","text":"<pre><code>forward(time: Tensor) -&gt; Tensor\n</code></pre> <p>Map scalar time values to a target-dimensioned embedding vector.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>Tensor</code> <p>Timestep tensor of shape (batch,) or (batch, 1).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Embedding tensor of shape (batch, embedding_dim).</p>"},{"location":"api/transformer/models/","title":"Transformer models","text":"<p><code>ml_suite.models.transformer</code> provides token-centric transformer architectures. All models operate on token tensors shaped <code>(batch, tokens, dim)</code> and enter and exit via tokenizers and heads.</p> <p>This module contains five model classes. <code>TokenToTokenTransformer</code> maps a sequence to a sequence of the same length. <code>TokenToVectorTransformer</code> collapses the sequence to a single vector via pooling. <code>TokenToClassTransformer</code> is the same but the output is a logit vector. <code>ConditionedTokenTransformer</code> is <code>TokenToTokenTransformer</code> with time, class, and context conditioning. <code>PatchTransformerND</code> handles spatial grid inputs by splitting them into patches first. For common configurations, the preset functions in <code>models.transformer.presets</code> are easier to use than constructing these directly.</p>"},{"location":"api/transformer/models/#tokentotokentransformer","title":"TokenToTokenTransformer","text":"<p>Use this when you want each input token to produce one output token, for example flow-matching velocity estimation on point sets.</p>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.TokenToTokenTransformer","title":"TokenToTokenTransformer","text":"<pre><code>TokenToTokenTransformer(input_dim: int, output_dim: int, embedding_dim: int, depth: int, num_heads: int, max_length: int | None = None, positional_encoding: PositionalEncodingMode = 'learned', mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Generic token-to-token transformer.</p> Input <p>x.shape == (batch, tokens, input_dim)</p> Output <p>out.shape == (batch, tokens, output_dim)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.models import TokenToTokenTransformer\n&gt;&gt;&gt; model = TokenToTokenTransformer(\n...     input_dim=32, output_dim=8, embedding_dim=128, depth=4, num_heads=4\n... )\n&gt;&gt;&gt; x = torch.randn(2, 16, 32)  # (batch, tokens, input_dim)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 16, 8])\n</code></pre>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.TokenToTokenTransformer.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Map a token sequence to output tokens of the same length.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input token tensor of shape (batch, tokens, input_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean valid-token mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, tokens, output_dim).</p>"},{"location":"api/transformer/models/#tokentovectortransformer","title":"TokenToVectorTransformer","text":"<p>Use this when you want the whole sequence collapsed to one vector, for example encoding a variable-length sequence for downstream regression.</p>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.TokenToVectorTransformer","title":"TokenToVectorTransformer","text":"<pre><code>TokenToVectorTransformer(input_dim: int, output_dim: int, embedding_dim: int, depth: int, num_heads: int, pooling: PoolingMode = 'mean', max_length: int | None = None, positional_encoding: PositionalEncodingMode = 'learned', mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Generic token-to-vector transformer.</p> <p>Collapses the token sequence to a single vector via pooling.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.models import TokenToVectorTransformer\n&gt;&gt;&gt; model = TokenToVectorTransformer(\n...     input_dim=32, output_dim=64, embedding_dim=128, depth=4, num_heads=4\n... )\n&gt;&gt;&gt; x = torch.randn(2, 16, 32)  # (batch, tokens, input_dim)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 64])\n</code></pre>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.TokenToVectorTransformer.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Map a token sequence to a single output vector.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input token tensor of shape (batch, tokens, input_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean valid-token mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output vector of shape (batch, output_dim).</p>"},{"location":"api/transformer/models/#tokentoclasstransformer","title":"TokenToClassTransformer","text":"<p>Use this for classification tasks. It is a thin wrapper over <code>TokenToVectorTransformer</code> that names the output dimension <code>num_classes</code>.</p>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.TokenToClassTransformer","title":"TokenToClassTransformer","text":"<pre><code>TokenToClassTransformer(input_dim: int, num_classes: int, embedding_dim: int, depth: int, num_heads: int, pooling: PoolingMode = 'mean', max_length: int | None = None, positional_encoding: PositionalEncodingMode = 'learned', mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False)\n</code></pre> <p>               Bases: <code>TokenToVectorTransformer</code></p> <p>Generic token-to-class transformer.</p> <p>A thin wrapper over TokenToVectorTransformer that names the output dimension <code>num_classes</code> and validates that it is positive.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.models import TokenToClassTransformer\n&gt;&gt;&gt; model = TokenToClassTransformer(\n...     input_dim=32, num_classes=10, embedding_dim=128, depth=4, num_heads=4\n... )\n&gt;&gt;&gt; x = torch.randn(2, 16, 32)  # (batch, tokens, input_dim)\n&gt;&gt;&gt; logits = model(x)\n&gt;&gt;&gt; logits.shape\ntorch.Size([2, 10])\n</code></pre>"},{"location":"api/transformer/models/#conditionedtokentransformer","title":"ConditionedTokenTransformer","text":"<p>Use this for score or velocity field estimation in diffusion or flow-matching models operating on token sequences. Conditioning signals are summed and broadcast-added to every token before the transformer stack.</p> <p>Note that conditioning is global, not per-token. All global signals (<code>time</code>, <code>class_labels</code>, <code>global_context</code>) are projected to <code>embedding_dim</code>, summed, and added to every token in the sequence. If you need per-token conditioning, use cross-attention via <code>cross_attention_dim</code> instead.</p>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.ConditionedTokenTransformer","title":"ConditionedTokenTransformer","text":"<pre><code>ConditionedTokenTransformer(input_dim: int, output_dim: int, embedding_dim: int, depth: int, num_heads: int, max_length: int | None = None, positional_encoding: PositionalEncodingMode = 'learned', time_conditioning: bool = False, time_embedding_type: TimeEmbeddingType = 'sinusoidal', num_classes: int | None = None, class_dropout_prob: float = 0.0, global_context_dim: int | None = None, cross_attention_dim: int | None = None, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Conditional token-to-token transformer.</p> <p>Global conditioning sources (time, class labels, global context) are projected to <code>embedding_dim</code>, summed, and broadcast-added to every token before the transformer stack. For per-token conditioning, use <code>cross_attention_dim</code> instead.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.models import ConditionedTokenTransformer\n&gt;&gt;&gt; model = ConditionedTokenTransformer(\n...     input_dim=3, output_dim=3, embedding_dim=128, depth=4, num_heads=4,\n...     positional_encoding='none', time_conditioning=True\n... )\n&gt;&gt;&gt; x = torch.randn(2, 512, 3)  # (batch, points, xyz)\n&gt;&gt;&gt; t = torch.rand(2)\n&gt;&gt;&gt; out = model(x, time=t)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 512, 3])\n</code></pre>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.ConditionedTokenTransformer.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None, time: Tensor | None = None, class_labels: Tensor | None = None, global_context: Tensor | None = None, cross_context: Tensor | None = None, cross_context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Map a conditioned token sequence to output tokens of the same length.</p> <p>Global conditioning sources (time, class, context) are projected to embedding_dim, summed, and broadcast-added to all tokens before the transformer stack.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Input token tensor of shape (batch, tokens, input_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean valid-token mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <code>time</code> <code>Tensor | None</code> <p>Diffusion timestep of shape (batch,). Required when time_conditioning=True.</p> <code>None</code> <code>class_labels</code> <code>Tensor | None</code> <p>Integer class indices of shape (batch,). Required when num_classes is set.</p> <code>None</code> <code>global_context</code> <code>Tensor | None</code> <p>Global context vector of shape (batch, global_context_dim). Required when global_context_dim is set.</p> <code>None</code> <code>cross_context</code> <code>Tensor | None</code> <p>Cross-attention token sequence of shape (batch, context_tokens, cross_attention_dim). Required when cross_attention_dim is set.</p> <code>None</code> <code>cross_context_mask</code> <code>Tensor | None</code> <p>Boolean mask of shape (batch, context_tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, tokens, output_dim).</p>"},{"location":"api/transformer/models/#patchtransformernd","title":"PatchTransformerND","text":"<p>Use this for grid-structured inputs where you want the transformer to operate on spatial patches rather than individual spatial positions. The <code>output_mode</code> parameter controls whether you get back a grid, a token sequence, or a single pooled vector.</p>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.PatchTransformerND","title":"PatchTransformerND","text":"<pre><code>PatchTransformerND(input_dim: int, in_channels: int, out_channels: int | None, patch_size: int | Sequence[int], embedding_dim: int, depth: int, num_heads: int, output_mode: PatchOutputMode = 'grid', vector_output_dim: int | None = None, max_length: int | None = None, positional_encoding: PositionalEncodingMode = 'sinusoidal', pooling: PoolingMode = 'mean', mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Transformer over 1D, 2D, or 3D patches.</p> <p>Splits a dense spatial grid into non-overlapping patches, processes them with a transformer stack, and decodes the result. <code>output_mode</code> controls the output shape: <code>'grid'</code> restores the original spatial dimensions, <code>'vector'</code> produces a single pooled vector, and <code>'tokens'</code> returns the raw patch token sequence.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.models import PatchTransformerND\n&gt;&gt;&gt; model = PatchTransformerND(\n...     input_dim=2, in_channels=3, out_channels=1,\n...     patch_size=16, embedding_dim=256, depth=4, num_heads=8\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)  # (batch, channels, H, W)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 1, 64, 64])\n</code></pre>"},{"location":"api/transformer/models/#ml_suite.models.transformer.models.PatchTransformerND.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Patchify the input grid, process with the transformer stack, and decode.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Spatial input of shape (batch, in_channels, *spatial). Each spatial dimension must be divisible by the corresponding patch_size entry.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Decoded output whose shape depends on output_mode: - 'grid': (batch, out_channels, *spatial) - 'tokens': (batch, num_patches, out_channels) or     (batch, num_patches, embedding_dim) if out_channels is None - 'vector': (batch, vector_output_dim)</p>"},{"location":"api/transformer/presets/","title":"Transformer presets","text":"<p><code>ml_suite.models.transformer.presets</code> contains factory functions that assemble fully configured transformer models for common tasks. Each function is a thin wrapper over the core model classes and returns a ready-to-use <code>nn.Module</code>.</p>"},{"location":"api/transformer/presets/#make_point_cloud_classifier","title":"make_point_cloud_classifier","text":"<p>No positional encoding is applied because point clouds are unordered and predictions should be permutation-invariant.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_point_cloud_classifier","title":"make_point_cloud_classifier","text":"<pre><code>make_point_cloud_classifier(point_dim: int, num_classes: int, embedding_dim: int, depth: int, num_heads: int, feature_dim: int = 0, pooling: PoolingMode = 'mean') -&gt; TokenToClassTransformer\n</code></pre> <p>Create a permutation-invariant point-cloud classifier.</p> <p>No positional encoding is applied, so predictions are invariant to point ordering. Each point is treated as a token; global pooling aggregates them before classification.</p> <p>Parameters:</p> Name Type Description Default <code>point_dim</code> <code>int</code> <p>Spatial dimension of each point (e.g. 3 for XYZ coordinates).</p> required <code>num_classes</code> <code>int</code> <p>Number of output classes.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <code>feature_dim</code> <code>int</code> <p>Optional per-point feature dimension appended to coordinates. The effective input dimension becomes point_dim + feature_dim.</p> <code>0</code> <code>pooling</code> <code>PoolingMode</code> <p>Pooling strategy applied over tokens before the classification head.</p> <code>'mean'</code> <p>Returns:</p> Type Description <code>TokenToClassTransformer</code> <p>A TokenToClassTransformer configured for point-cloud classification.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_point_cloud_classifier\n&gt;&gt;&gt; model = make_point_cloud_classifier(\n...     point_dim=3, num_classes=40, embedding_dim=128, depth=4, num_heads=4\n... )\n&gt;&gt;&gt; x = torch.randn(2, 512, 3)  # (batch, points, xyz)\n&gt;&gt;&gt; logits = model(x)\n&gt;&gt;&gt; logits.shape\ntorch.Size([2, 40])\n</code></pre>"},{"location":"api/transformer/presets/#make_point_to_point_model","title":"make_point_to_point_model","text":"<p>No positional encoding is applied because point clouds are unordered and per-point outputs should be permutation-equivariant.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_point_to_point_model","title":"make_point_to_point_model","text":"<pre><code>make_point_to_point_model(point_dim: int, output_dim: int, embedding_dim: int, depth: int, num_heads: int, feature_dim: int = 0) -&gt; TokenToTokenTransformer\n</code></pre> <p>Create a permutation-equivariant point-to-point model.</p> <p>No positional encoding is applied, so predictions are equivariant to point ordering. Each input point produces one output vector of output_dim features.</p> <p>Parameters:</p> Name Type Description Default <code>point_dim</code> <code>int</code> <p>Spatial dimension of each input point (e.g. 3 for XYZ).</p> required <code>output_dim</code> <code>int</code> <p>Output feature dimension per point.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <code>feature_dim</code> <code>int</code> <p>Optional per-point feature dimension appended to coordinates. The effective input dimension becomes point_dim + feature_dim.</p> <code>0</code> <p>Returns:</p> Type Description <code>TokenToTokenTransformer</code> <p>A TokenToTokenTransformer configured for point-to-point prediction.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_point_to_point_model\n&gt;&gt;&gt; model = make_point_to_point_model(\n...     point_dim=3, output_dim=3, embedding_dim=128, depth=4, num_heads=4\n... )\n&gt;&gt;&gt; x = torch.randn(2, 512, 3)  # (batch, points, xyz)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 512, 3])\n</code></pre>"},{"location":"api/transformer/presets/#make_conditioned_point_to_point_model","title":"make_conditioned_point_to_point_model","text":"<p>No positional encoding is applied because point clouds are unordered and per-point outputs should be permutation-equivariant.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_conditioned_point_to_point_model","title":"make_conditioned_point_to_point_model","text":"<pre><code>make_conditioned_point_to_point_model(point_dim: int, output_dim: int, embedding_dim: int, depth: int, num_heads: int, feature_dim: int = 0, time_conditioning: bool = False, global_context_dim: int | None = None, cross_attention_dim: int | None = None) -&gt; ConditionedTokenTransformer\n</code></pre> <p>Create a conditional point-to-point model.</p> <p>Suitable for score or velocity field estimation in diffusion and flow-matching models operating on point sets. No positional encoding is applied to the point tokens.</p> <p>Parameters:</p> Name Type Description Default <code>point_dim</code> <code>int</code> <p>Spatial dimension of each input point (e.g. 3 for XYZ).</p> required <code>output_dim</code> <code>int</code> <p>Output feature dimension per point.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <code>feature_dim</code> <code>int</code> <p>Optional per-point feature dimension appended to coordinates. The effective input dimension becomes point_dim + feature_dim.</p> <code>0</code> <code>time_conditioning</code> <code>bool</code> <p>If True, accept a diffusion timestep tensor of shape (batch,).</p> <code>False</code> <code>global_context_dim</code> <code>int | None</code> <p>Dimension of an optional global context vector.</p> <code>None</code> <code>cross_attention_dim</code> <code>int | None</code> <p>If set, enable cross-attention with an external token sequence of this feature dimension.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConditionedTokenTransformer</code> <p>A ConditionedTokenTransformer configured for conditioned point-to-point prediction.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_conditioned_point_to_point_model\n&gt;&gt;&gt; model = make_conditioned_point_to_point_model(\n...     point_dim=3, output_dim=3, embedding_dim=128,\n...     depth=4, num_heads=4, time_conditioning=True\n... )\n&gt;&gt;&gt; x = torch.randn(2, 512, 3)  # (batch, points, xyz)\n&gt;&gt;&gt; t = torch.rand(2)\n&gt;&gt;&gt; velocity = model(x, time=t)\n&gt;&gt;&gt; velocity.shape\ntorch.Size([2, 512, 3])\n</code></pre>"},{"location":"api/transformer/presets/#make_sequence_classifier","title":"make_sequence_classifier","text":"<p>RoPE is used here because it encodes relative positions inside attention rather than as an additive vector, which generalizes better to variable-length sequences.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_sequence_classifier","title":"make_sequence_classifier","text":"<pre><code>make_sequence_classifier(input_dim: int, num_classes: int, embedding_dim: int, depth: int, num_heads: int, max_length: int, pooling: PoolingMode = 'mean') -&gt; TokenToClassTransformer\n</code></pre> <p>Create an ordered sequence classifier.</p> <p>Uses RoPE positional encoding so position information is injected inside the attention mechanism rather than as an additive embedding. Suitable for variable-length sequences.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Feature dimension of each input token.</p> required <code>num_classes</code> <code>int</code> <p>Number of output classes.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <code>max_length</code> <code>int</code> <p>Maximum sequence length. Used to bound RoPE frequency computations.</p> required <code>pooling</code> <code>PoolingMode</code> <p>Pooling strategy applied over tokens before the classification head.</p> <code>'mean'</code> <p>Returns:</p> Type Description <code>TokenToClassTransformer</code> <p>A TokenToClassTransformer configured for sequence classification with RoPE.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_sequence_classifier\n&gt;&gt;&gt; model = make_sequence_classifier(\n...     input_dim=64, num_classes=10, embedding_dim=128,\n...     depth=4, num_heads=4, max_length=512\n... )\n&gt;&gt;&gt; x = torch.randn(2, 128, 64)  # (batch, tokens, features)\n&gt;&gt;&gt; logits = model(x)\n&gt;&gt;&gt; logits.shape\ntorch.Size([2, 10])\n</code></pre>"},{"location":"api/transformer/presets/#make_patch_grid_model","title":"make_patch_grid_model","text":"<p>Sinusoidal encoding is used because patch grids have a fixed spatial structure and sinusoidal embeddings generalize to unseen grid sizes without retraining.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_patch_grid_model","title":"make_patch_grid_model","text":"<pre><code>make_patch_grid_model(input_dim: int, in_channels: int, out_channels: int, patch_size: int | tuple[int, ...], embedding_dim: int, depth: int, num_heads: int) -&gt; PatchTransformerND\n</code></pre> <p>Create a grid-to-grid patch transformer.</p> <p>The input dense spatial grid is split into non-overlapping patches, each embedded as a token. After transformer processing, patch tokens are decoded back to a dense spatial grid of the same resolution. Sinusoidal positional encoding is applied to the patch tokens.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Number of spatial dimensions (1, 2, or 3).</p> required <code>in_channels</code> <code>int</code> <p>Number of input channels.</p> required <code>out_channels</code> <code>int</code> <p>Number of output channels in the reconstructed grid.</p> required <code>patch_size</code> <code>int | tuple[int, ...]</code> <p>Patch size per spatial dimension. A single int is broadcast to all dimensions. The spatial dimensions of the input must be divisible by this value.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <p>Returns:</p> Type Description <code>PatchTransformerND</code> <p>A PatchTransformerND configured for grid-to-grid prediction.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_patch_grid_model\n&gt;&gt;&gt; model = make_patch_grid_model(\n...     input_dim=2, in_channels=3, out_channels=1,\n...     patch_size=16, embedding_dim=256, depth=4, num_heads=8\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)  # (batch, channels, H, W)\n&gt;&gt;&gt; out = model(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 1, 64, 64])\n</code></pre>"},{"location":"api/transformer/presets/#make_patch_classifier","title":"make_patch_classifier","text":"<p>Sinusoidal encoding is used because patch grids have a fixed spatial structure and sinusoidal embeddings generalize to unseen grid sizes without retraining.</p>"},{"location":"api/transformer/presets/#ml_suite.models.transformer.presets.make_patch_classifier","title":"make_patch_classifier","text":"<pre><code>make_patch_classifier(input_dim: int, in_channels: int, num_classes: int, patch_size: int | tuple[int, ...], embedding_dim: int, depth: int, num_heads: int) -&gt; PatchTransformerND\n</code></pre> <p>Create a patch-based classifier for 1D, 2D, or 3D grids.</p> <p>The input dense spatial grid is split into non-overlapping patches, each embedded as a token. After transformer processing, patch tokens are pooled into a single class prediction vector. Sinusoidal positional encoding is applied to the patch tokens.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Number of spatial dimensions (1, 2, or 3).</p> required <code>in_channels</code> <code>int</code> <p>Number of input channels.</p> required <code>num_classes</code> <code>int</code> <p>Number of output classes.</p> required <code>patch_size</code> <code>int | tuple[int, ...]</code> <p>Patch size per spatial dimension. A single int is broadcast to all dimensions. The spatial dimensions of the input must be divisible by this value.</p> required <code>embedding_dim</code> <code>int</code> <p>Transformer embedding dimension.</p> required <code>depth</code> <code>int</code> <p>Number of transformer blocks.</p> required <code>num_heads</code> <code>int</code> <p>Number of attention heads per block.</p> required <p>Returns:</p> Type Description <code>PatchTransformerND</code> <p>A PatchTransformerND configured for patch-based classification.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.presets import make_patch_classifier\n&gt;&gt;&gt; model = make_patch_classifier(\n...     input_dim=2, in_channels=3, num_classes=10,\n...     patch_size=16, embedding_dim=256, depth=4, num_heads=8\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)  # (batch, channels, H, W)\n&gt;&gt;&gt; logits = model(x)\n&gt;&gt;&gt; logits.shape\ntorch.Size([2, 10])\n</code></pre>"},{"location":"api/transformer/primitives/","title":"Transformer primitives","text":"<p>These are the low-level components that the model classes in <code>models.transformer</code> are assembled from. You only need to read this page if you are building a non-standard architecture by hand. For everything else, start from the presets or models pages.</p>"},{"location":"api/transformer/primitives/#attention","title":"Attention","text":""},{"location":"api/transformer/primitives/#ml_suite.models.transformer.attention","title":"attention","text":"<p>Multi-head self-attention and cross-attention primitives.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.attention.MultiHeadSelfAttention","title":"MultiHeadSelfAttention","text":"<pre><code>MultiHeadSelfAttention(embedding_dim: int, num_heads: int, head_dim: int | None = None, dropout: float = 0.0, causal: bool = False, positional_encoding: PositionalEncodingMode = 'none', rope_base: float = 10000.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Batch-first multi-head self-attention.</p> <p>Uses :func:<code>torch.nn.functional.scaled_dot_product_attention</code> which uses FlashAttention (CUDA fp16/bf16), memory-efficient attention, or a math fallback automatically.  On MPS the fused MPS SDPA path is used.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.attention.MultiHeadSelfAttention.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply multi-head self-attention to a token tensor.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean valid-token mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, tokens, embedding_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.attention.MultiHeadCrossAttention","title":"MultiHeadCrossAttention","text":"<pre><code>MultiHeadCrossAttention(query_dim: int, context_dim: int, num_heads: int, head_dim: int | None = None, dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Batch-first multi-head cross-attention.</p> <p>Uses :func:<code>torch.nn.functional.scaled_dot_product_attention</code> which dispatches to FlashAttention (CUDA fp16/bf16), memory-efficient attention, or a math fallback automatically.  On MPS the fused MPS SDPA path is used.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.attention.MultiHeadCrossAttention.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor, context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply multi-head cross-attention from query tokens to context tokens.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Query token tensor of shape (batch, query_tokens, query_dim).</p> required <code>context</code> <code>Tensor</code> <p>Context token tensor of shape (batch, context_tokens, context_dim).</p> required <code>context_mask</code> <code>Tensor | None</code> <p>Optional boolean mask of shape (batch, context_tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, query_tokens, query_dim).</p>"},{"location":"api/transformer/primitives/#blocks-and-stack","title":"Blocks and stack","text":""},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks","title":"blocks","text":"<p>Transformer feed-forward and residual attention blocks.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks.FeedForward","title":"FeedForward","text":"<pre><code>FeedForward(embedding_dim: int, hidden_dim: int, activation: str = 'gelu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Transformer feed-forward network.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks.FeedForward.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Apply the two-layer feed-forward network to tokens.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of the same shape.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks.TransformerBlock","title":"TransformerBlock","text":"<pre><code>TransformerBlock(embedding_dim: int, num_heads: int, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False, use_cross_attention: bool = False, cross_attention_dim: int | None = None, norm_type: NormType = 'layer', positional_encoding: PositionalEncodingMode = 'none')\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Pre-norm transformer block with optional cross-attention.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks.TransformerBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None, context: Tensor | None = None, context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply pre-norm self-attention, optional cross-attention, and feed-forward with residuals.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean self-attention mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <code>context</code> <code>Tensor | None</code> <p>Cross-attention context of shape (batch, context_tokens, cross_attention_dim). Required when use_cross_attention=True.</p> <code>None</code> <code>context_mask</code> <code>Tensor | None</code> <p>Optional boolean mask of shape (batch, context_tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, tokens, embedding_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.blocks.build_norm","title":"build_norm","text":"<pre><code>build_norm(norm_type: NormType, embedding_dim: int) -&gt; Module\n</code></pre> <p>Instantiate a normalisation layer by type name.</p> <p>Parameters:</p> Name Type Description Default <code>norm_type</code> <code>NormType</code> <p>'layer' for LayerNorm or 'rms' for RMSNorm.</p> required <code>embedding_dim</code> <code>int</code> <p>Feature dimension to normalise over.</p> required <p>Returns:</p> Type Description <code>Module</code> <p>The requested normalisation module.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If norm_type is not supported.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.stacks","title":"stacks","text":"<p>Transformer block stacks.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.stacks.TransformerStack","title":"TransformerStack","text":"<pre><code>TransformerStack(embedding_dim: int, depth: int, num_heads: int, mlp_ratio: float = 4.0, dropout: float = 0.0, attention_dropout: float = 0.0, causal: bool = False, use_cross_attention: bool = False, cross_attention_dim: int | None = None, norm_type: NormType = 'layer', positional_encoding: PositionalEncodingMode = 'none', final_norm: bool = True)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Stack of pre-norm transformer blocks.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.stacks import TransformerStack\n&gt;&gt;&gt; stack = TransformerStack(embedding_dim=256, depth=4, num_heads=8)\n&gt;&gt;&gt; x = torch.randn(2, 32, 256)  # (batch, tokens, embedding_dim)\n&gt;&gt;&gt; out = stack(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 32, 256])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.stacks.TransformerStack.forward","title":"forward","text":"<pre><code>forward(x: Tensor, mask: Tensor | None = None, context: Tensor | None = None, context_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Apply all transformer blocks to a token tensor.</p>"},{"location":"api/transformer/primitives/#tokenizers","title":"Tokenizers","text":"<p>These tokenizers convert raw inputs into the <code>(batch, tokens, embedding_dim)</code> format the transformer expects. Use <code>ContinuousInputTokenizer</code> for float feature vectors. Use <code>PatchTokenizerND</code> for spatial grids. Use <code>DiscreteTokenTokenizer</code> for integer token IDs. <code>SetTokenizer</code> is identical to <code>ContinuousInputTokenizer</code> and exists as a semantic alias for unordered inputs.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization","title":"tokenization","text":"<p>Input tokenizers for continuous, discrete, set, and patch-structured data.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.ContinuousInputTokenizer","title":"ContinuousInputTokenizer","text":"<pre><code>ContinuousInputTokenizer(input_dim: int, embedding_dim: int, num_layers: int = 1, hidden_dim: int | None = None, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Project continuous token features into transformer embedding space.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.tokenization import ContinuousInputTokenizer\n&gt;&gt;&gt; tokenizer = ContinuousInputTokenizer(input_dim=32, embedding_dim=128)\n&gt;&gt;&gt; x = torch.randn(4, 16, 32)  # (batch, tokens, features)\n&gt;&gt;&gt; out = tokenizer(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 16, 128])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.ContinuousInputTokenizer.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Tokenize a continuous tensor of shape (batch, tokens, input_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.SetTokenizer","title":"SetTokenizer","text":"<pre><code>SetTokenizer(input_dim: int, embedding_dim: int, num_layers: int = 1, hidden_dim: int | None = None, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>ContinuousInputTokenizer</code></p> <p>Tokenizer for unordered set-like inputs.</p> <p>Identical to ContinuousInputTokenizer. The different name signals intent: use this when the input has no meaningful ordering, such as a point cloud.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.tokenization import SetTokenizer\n&gt;&gt;&gt; tokenizer = SetTokenizer(input_dim=64, embedding_dim=256)\n&gt;&gt;&gt; points = torch.randn(2, 512, 64)  # (batch, points, features)\n&gt;&gt;&gt; out = tokenizer(points)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 512, 256])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.DiscreteTokenTokenizer","title":"DiscreteTokenTokenizer","text":"<pre><code>DiscreteTokenTokenizer(vocab_size: int, embedding_dim: int, padding_idx: int | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Embed integer token IDs.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.tokenization import DiscreteTokenTokenizer\n&gt;&gt;&gt; tokenizer = DiscreteTokenTokenizer(vocab_size=1000, embedding_dim=128)\n&gt;&gt;&gt; ids = torch.randint(0, 1000, (4, 32))  # (batch, tokens)\n&gt;&gt;&gt; out = tokenizer(ids)\n&gt;&gt;&gt; out.shape\ntorch.Size([4, 32, 128])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.DiscreteTokenTokenizer.forward","title":"forward","text":"<pre><code>forward(token_ids: Tensor) -&gt; Tensor\n</code></pre> <p>Embed token IDs shaped (batch, tokens).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.PatchTokenizerND","title":"PatchTokenizerND","text":"<pre><code>PatchTokenizerND(spatial_dim: int, in_channels: int, embedding_dim: int, patch_size: int | Sequence[int])\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Patchify 1D, 2D, or 3D grid data into token embeddings.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.tokenization import PatchTokenizerND\n&gt;&gt;&gt; tokenizer = PatchTokenizerND(\n...     spatial_dim=2, in_channels=3, embedding_dim=256, patch_size=16\n... )\n&gt;&gt;&gt; x = torch.randn(2, 3, 64, 64)  # (batch, channels, H, W)\n&gt;&gt;&gt; tokens, grid_shape = tokenizer(x)\n&gt;&gt;&gt; tokens.shape   # (batch, num_patches, embedding_dim)\ntorch.Size([2, 16, 256])\n&gt;&gt;&gt; grid_shape     # patches per spatial dimension\n(4, 4)\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.tokenization.PatchTokenizerND.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; tuple[Tensor, tuple[int, ...]]\n</code></pre> <p>Return patch tokens and the flattened patch-grid shape.</p>"},{"location":"api/transformer/primitives/#positional-encodings","title":"Positional encodings","text":"<p>Use <code>SinusoidalPositionalEmbedding</code> for patch transformers and other tasks where you want a fixed encoding that generalizes to unseen sequence lengths. Use <code>LearnedPositionalEmbedding</code> when sequence lengths are fixed and the positions can be learned. Use <code>RotaryEmbedding</code> via the <code>positional_encoding='rope'</code> argument to self-attention when you want position-relative attention without an additive embedding, which is standard for sequence models. Pass <code>positional_encoding='none'</code> for point clouds and other inputs where order does not matter.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional","title":"positional","text":"<p>Absolute and rotary positional encodings.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.LearnedPositionalEmbedding","title":"LearnedPositionalEmbedding","text":"<pre><code>LearnedPositionalEmbedding(max_length: int, embedding_dim: int)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Learned absolute positional embedding for token tensors.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.positional import LearnedPositionalEmbedding\n&gt;&gt;&gt; pe = LearnedPositionalEmbedding(max_length=512, embedding_dim=128)\n&gt;&gt;&gt; x = torch.randn(2, 64, 128)  # (batch, tokens, embedding_dim)\n&gt;&gt;&gt; out = pe(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 64, 128])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.LearnedPositionalEmbedding.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Add learned positional embeddings to a token tensor.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Tensor of the same shape with positional embeddings added.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.SinusoidalPositionalEmbedding","title":"SinusoidalPositionalEmbedding","text":"<pre><code>SinusoidalPositionalEmbedding(embedding_dim: int, max_length: int | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Fixed absolute sinusoidal positional embedding.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.positional import SinusoidalPositionalEmbedding\n&gt;&gt;&gt; pe = SinusoidalPositionalEmbedding(embedding_dim=128)\n&gt;&gt;&gt; x = torch.randn(2, 64, 128)  # (batch, tokens, embedding_dim)\n&gt;&gt;&gt; out = pe(x)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 64, 128])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.SinusoidalPositionalEmbedding.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> <p>Add fixed sinusoidal positional embeddings to a token tensor.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Tensor of the same shape with positional embeddings added.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.RotaryEmbedding","title":"RotaryEmbedding","text":"<pre><code>RotaryEmbedding(head_dim: int, base: float = 10000.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Rotary positional embedding cache for attention Q/K tensors.</p> <p>Unlike additive embeddings, RoPE is applied inside attention to the Q and K tensors directly. Call this module to get the (cos, sin) rotation matrices, then pass them to the attention layer via the <code>positional_encoding='rope'</code> path.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.positional import RotaryEmbedding\n&gt;&gt;&gt; rope = RotaryEmbedding(head_dim=64)\n&gt;&gt;&gt; cos, sin = rope(seq_len=32, device=torch.device('cpu'), dtype=torch.float32)\n&gt;&gt;&gt; cos.shape  # (seq_len, head_dim // 2)\ntorch.Size([32, 32])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.RotaryEmbedding.forward","title":"forward","text":"<pre><code>forward(seq_len: int, device: device, dtype: dtype) -&gt; tuple[Tensor, Tensor]\n</code></pre> <p>Return cosine and sine rotation matrices for a given sequence length.</p> <p>Parameters:</p> Name Type Description Default <code>seq_len</code> <code>int</code> <p>Number of positions to generate embeddings for.</p> required <code>device</code> <code>device</code> <p>Target device for the output tensors.</p> required <code>dtype</code> <code>dtype</code> <p>Target dtype for the output tensors.</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>A pair (cos, sin), each of shape (seq_len, rotary_dim // 2),</p> <code>Tensor</code> <p>where rotary_dim is head_dim rounded down to the nearest even number.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.apply_rope_to_tensor","title":"apply_rope_to_tensor","text":"<pre><code>apply_rope_to_tensor(x: Tensor, cos: Tensor, sin: Tensor) -&gt; Tensor\n</code></pre> <p>Apply RoPE to a tensor shaped (batch, heads, tokens, head_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.positional.build_absolute_positional_embedding","title":"build_absolute_positional_embedding","text":"<pre><code>build_absolute_positional_embedding(mode: PositionalEncodingMode, embedding_dim: int, max_length: int | None) -&gt; Module\n</code></pre> <p>Build an additive absolute positional embedding module.</p>"},{"location":"api/transformer/primitives/#heads","title":"Heads","text":""},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads","title":"heads","text":"<p>Task heads for token and pooled transformer outputs.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.TokenwiseHead","title":"TokenwiseHead","text":"<pre><code>TokenwiseHead(embedding_dim: int, output_dim: int, hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Map each token independently to an output dimension.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.heads import TokenwiseHead\n&gt;&gt;&gt; head = TokenwiseHead(embedding_dim=256, output_dim=3)\n&gt;&gt;&gt; tokens = torch.randn(2, 512, 256)  # (batch, tokens, embedding_dim)\n&gt;&gt;&gt; out = head(tokens)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 512, 3])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.TokenwiseHead.forward","title":"forward","text":"<pre><code>forward(tokens: Tensor) -&gt; Tensor\n</code></pre> <p>Map each token independently to the output dimension.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output tensor of shape (batch, tokens, output_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.PooledHead","title":"PooledHead","text":"<pre><code>PooledHead(embedding_dim: int, output_dim: int, pooling: PoolingMode = 'mean', hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Pool tokens and map the pooled vector to an output dimension.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import torch\n&gt;&gt;&gt; from ml_suite.models.transformer.heads import PooledHead\n&gt;&gt;&gt; head = PooledHead(embedding_dim=256, output_dim=10)\n&gt;&gt;&gt; tokens = torch.randn(2, 32, 256)  # (batch, tokens, embedding_dim)\n&gt;&gt;&gt; out = head(tokens)\n&gt;&gt;&gt; out.shape\ntorch.Size([2, 10])\n</code></pre>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.PooledHead.forward","title":"forward","text":"<pre><code>forward(tokens: Tensor, mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Pool tokens and map the pooled vector to the output dimension.</p> <p>Parameters:</p> Name Type Description Default <code>tokens</code> <code>Tensor</code> <p>Token tensor of shape (batch, tokens, embedding_dim).</p> required <code>mask</code> <code>Tensor | None</code> <p>Optional boolean valid-token mask of shape (batch, tokens). True = valid token.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Output vector of shape (batch, output_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.ClassificationHead","title":"ClassificationHead","text":"<pre><code>ClassificationHead(embedding_dim: int, output_dim: int, pooling: PoolingMode = 'mean', hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>PooledHead</code></p> <p>Pooled classification head.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.RegressionHead","title":"RegressionHead","text":"<pre><code>RegressionHead(embedding_dim: int, output_dim: int, pooling: PoolingMode = 'mean', hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>PooledHead</code></p> <p>Pooled regression head.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.heads.make_head_mlp","title":"make_head_mlp","text":"<pre><code>make_head_mlp(input_dim: int, output_dim: int, hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0) -&gt; Module\n</code></pre> <p>Build a small MLP head.</p> <p>Parameters:</p> Name Type Description Default <code>input_dim</code> <code>int</code> <p>Input feature dimension.</p> required <code>output_dim</code> <code>int</code> <p>Output feature dimension.</p> required <code>hidden_dim</code> <code>int | None</code> <p>Hidden layer dimension for num_layers &gt; 1. Defaults to input_dim.</p> <code>None</code> <code>num_layers</code> <code>int</code> <p>Total number of linear layers. 1 produces a single Linear with no activation.</p> <code>1</code> <code>activation</code> <code>str</code> <p>Activation function name applied between hidden layers.</p> <code>'silu'</code> <code>dropout</code> <code>float</code> <p>Dropout probability applied after each hidden activation and the final layer.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>Module</code> <p>An nn.Module that maps (..., input_dim) to (..., output_dim).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If input_dim, output_dim, or hidden_dim is non-positive, or num_layers &lt; 1.</p>"},{"location":"api/transformer/primitives/#decoders","title":"Decoders","text":""},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders","title":"decoders","text":"<p>Token decoders for per-token, grid, and query-set outputs.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders.TokenDecoder","title":"TokenDecoder","text":"<pre><code>TokenDecoder(embedding_dim: int, output_dim: int, hidden_dim: int | None = None, num_layers: int = 1, activation: str = 'silu', dropout: float = 0.0)\n</code></pre> <p>               Bases: <code>TokenwiseHead</code></p> <p>Decode each token independently.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders.PatchDecoderND","title":"PatchDecoderND","text":"<pre><code>PatchDecoderND(input_dim: int, embedding_dim: int, out_channels: int, patch_size: int | Sequence[int])\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Decode patch tokens back to a 1D, 2D, or 3D grid.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders.PatchDecoderND.forward","title":"forward","text":"<pre><code>forward(tokens: Tensor, grid_shape: Sequence[int]) -&gt; Tensor\n</code></pre> <p>Decode tokens shaped (batch, patches, embedding_dim) to a grid.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders.QuerySetDecoder","title":"QuerySetDecoder","text":"<pre><code>QuerySetDecoder(latent_dim: int, output_dim: int, num_queries: int, query_dim: int, hidden_dim: int | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Decode a global latent vector into a fixed-size set using learned queries.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.decoders.QuerySetDecoder.forward","title":"forward","text":"<pre><code>forward(latent: Tensor) -&gt; Tensor\n</code></pre> <p>Decode a latent vector into a fixed-size set of output tokens.</p> <p>Learned query vectors are biased by a projection of the latent before being mapped through the token-wise head to produce the output set.</p> <p>Parameters:</p> Name Type Description Default <code>latent</code> <code>Tensor</code> <p>Global latent vector of shape (batch, latent_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Output token set of shape (batch, num_queries, output_dim).</p>"},{"location":"api/transformer/primitives/#conditioning","title":"Conditioning","text":""},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning","title":"conditioning","text":"<p>Global conditioning utilities for token transformers.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning.TransformerConditioningBuilder","title":"TransformerConditioningBuilder","text":"<pre><code>TransformerConditioningBuilder(embedding_dim: int, time_conditioning: bool = False, time_embedding_type: TimeEmbeddingType = 'sinusoidal', num_classes: int | None = None, class_dropout_prob: float = 0.0, global_context_dim: int | None = None)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Build a single global conditioning vector.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning.TransformerConditioningBuilder.forward","title":"forward","text":"<pre><code>forward(batch_size: int, device: device, dtype: dtype, time: Tensor | None = None, class_labels: Tensor | None = None, global_context: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Return a conditioning vector shaped (batch, embedding_dim).</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning.TransformerConditioningBuilder.has_conditioning","title":"has_conditioning","text":"<pre><code>has_conditioning() -&gt; bool\n</code></pre> <p>Return True if any conditioning source is configured.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning.ConditionTokenProjector","title":"ConditionTokenProjector","text":"<pre><code>ConditionTokenProjector(context_dim: int, embedding_dim: int, num_tokens: int = 1)\n</code></pre> <p>               Bases: <code>Module</code></p> <p>Project a global context vector into one or more condition tokens.</p>"},{"location":"api/transformer/primitives/#ml_suite.models.transformer.conditioning.ConditionTokenProjector.forward","title":"forward","text":"<pre><code>forward(context: Tensor) -&gt; Tensor\n</code></pre> <p>Project a global context vector into one or more condition tokens.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Tensor</code> <p>Global context tensor of shape (batch, context_dim).</p> required <p>Returns:</p> Type Description <code>Tensor</code> <p>Condition tokens of shape (batch, num_tokens, embedding_dim).</p>"}]}